package lld2;

import java.util.List;

public class DBWriter implements Writer {

    @Override
    public void write(List<Record> records) {
        for (Record record : records) {
            // repository to store into
        }
    }
}
